{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "composer install --no-dev && php artisan optimize"
    },
    "devDependencies": {
        "axios": "^1.6.1",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.5.2"
    },
    "dependencies": {
        "@flaticon/flaticon-uicons": "^3.0.0",
        "@google/generative-ai": "^0.3.0"
    }
    
}
